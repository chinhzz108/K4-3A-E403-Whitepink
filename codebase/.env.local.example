# ScriptScout API keys — only placeholders; never put real keys in this sample.

# Groq primary
GROQ_API_KEY=

# Google Gemini fallback
GOOGLE_API_KEY=

# Optional web search; without it, search falls back to other engines.
SERPER_API_KEY=
